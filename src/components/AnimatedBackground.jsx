import React from 'react';

const AnimatedBackground = () => {
  return (
    <div className="fixed inset-0 -z-10 bg-gradient-to-br from-blue-900 via-purple-900 to-black bg-200 animate-gradient"></div>
  );
};

export default AnimatedBackground;
